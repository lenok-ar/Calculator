<Window x:Class="Calculator.RandomDataDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Генерация случайных данных" 
        Height="250" Width="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Тип данных:" FontWeight="SemiBold" Margin="0,0,0,5"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <RadioButton Name="IntegerRadio" Content="Целые числа" GroupName="DataType" IsChecked="True" Margin="0,0,10,0"/>
            <RadioButton Name="DoubleRadio" Content="Дробные числа" GroupName="DataType"/>
        </StackPanel>

        <TextBlock Grid.Row="2" Text="Диапазон значений:" FontWeight="SemiBold" Margin="0,0,0,5"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,5">
            <TextBlock Text="От:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBox Name="MinValueTextBox" Text="1" Width="60" Padding="3"/>
            <TextBlock Text="До:" VerticalAlignment="Center" Margin="10,0,5,0"/>
            <TextBox Name="MaxValueTextBox" Text="100" Width="60" Padding="3"/>
        </StackPanel>

        <TextBlock Grid.Row="4" Text="*Для дробных чисел будет 2 знака после запятой" 
                   FontStyle="Italic" Foreground="Gray" FontSize="10"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Отмена" Width="80" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button Content="ОК" Width="80" Background="#FF0078D7" Foreground="White" 
                    FontWeight="SemiBold" Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>